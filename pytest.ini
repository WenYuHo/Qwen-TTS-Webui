[pytest]
markers =
    browser: marks tests as browser tests (require playwright)
    integration: marks tests as integration tests
    load: marks tests as load tests
asyncio_mode = strict
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short --ignore=worker-1 --ignore=worker-2 --ignore=test-wt
